# SPDX-License-Identifier: AGPL-3.0-only
# SPDX-FileCopyrightText: 2024 Univention GmbH

{{- if .Values.managementApi.ingress.enabled -}}
---

apiVersion: {{ include "common.capabilities.ingress.apiVersion" . }}
kind: Ingress
metadata:
  name: {{ printf "%s-management-api" (include "common.names.fullname" .) }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
  {{- with .Values.managementApi.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.managementApi.ingress.ingressClassName }}
  ingressClassName: {{ coalesce .Values.managementApi.ingress.ingressClassName .Values.global.ingressClassName }}
  {{- end }}
  {{- if .Values.managementApi.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.managementApi.ingress.host | quote }}
      secretName: "{{ .Values.managementApi.ingress.tls.secretName }}"
  {{- end }}
  rules:
    - host: {{ .Values.managementApi.ingress.host | quote }}
      http:
        paths:
          {{- range .Values.managementApi.ingress.paths }}
          - pathType: {{ .pathType }}
            path: {{ .path }}
            {{- if .backend }}
            backend: {{- (toYaml .backend) | nindent 14 }}
            {{- else }}
            backend: {{- default (include "common.ingress.backend" (dict "serviceName" (include "common.names.fullname" $) "servicePort" "http" "context" $)) | nindent 14 }}
            {{- end }}
          {{- end }}

...
{{- end }}
