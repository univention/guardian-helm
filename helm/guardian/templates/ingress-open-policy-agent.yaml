# SPDX-License-Identifier: AGPL-3.0-only
# SPDX-FileCopyrightText: 2024 Univention GmbH

{{- if .Values.openPolicyAgent.ingress.enabled -}}
---

apiVersion: {{ include "common.capabilities.ingress.apiVersion" . }}
kind: Ingress
metadata:
  name: {{ printf "%s-open-policy-agent" (include "common.names.fullname" .) }}
  labels:
    {{- $commonLabels := include "common.labels.standard" . | fromYaml }}
    {{- $uiLabels := dict "app.kubernetes.io/name" (printf "%s-open-policy-agent" (include "common.names.fullname" .)) }}
    {{- $mergedLabels := merge $uiLabels $commonLabels}}
    {{- toYaml $mergedLabels | nindent 4 }}
  {{- with .Values.openPolicyAgent.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.openPolicyAgent.ingress.ingressClassName }}
  ingressClassName: {{ coalesce .Values.openPolicyAgent.ingress.ingressClassName .Values.global.ingressClassName }}
  {{- end }}
  {{- if .Values.openPolicyAgent.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ required "The \"ingress host\" is required." .Values.openPolicyAgent.ingress.host | quote }}
      secretName: "{{ .Values.openPolicyAgent.ingress.tls.secretName }}"
  {{- end }}
  rules:
    - host: {{ required "The \"ingress host\" is required." .Values.openPolicyAgent.ingress.host | quote }}
      http:
        paths:
          {{- range .Values.openPolicyAgent.ingress.paths }}
          - pathType: {{ .pathType }}
            path: {{ .path }}
            {{- if .backend }}
            backend: {{- (toYaml .backend) | nindent 14 }}
            {{- else }}
            backend: {{- default (include "common.ingress.backend" (dict "serviceName" (include "common.names.fullname" $) "servicePort" "http" "context" $)) | nindent 14 }}
            {{- end }}
          {{- end }}

...
{{- end }}
