---
{{ include "common.secret" (dict "top" . "overrides" "guardian-management-api.secret") }}

{{- define "guardian-management-api.secret" }}
{{- with .top }}
data:
  oauthAdapterM2mSecret: {{ .Values.guardianManagementApi.oauthAdapterM2mSecret | b64enc | quote }}
{{- end }}
{{- end }}
